# copy to .env, this just tells the frontend where the api lives
# for local development, use http://localhost:5000/api
# for Vercel deployment, set this to your hosted backend URL, e.g. https://your-backend-url/api
VITE_API_URL=/api
